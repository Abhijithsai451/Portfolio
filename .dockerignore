# Version control
.git
.gitignore

# IDE and Editor files
.idea/
.vscode/
*.swp
*.bak

# Python artifacts
__pycache__/
*.pyc
*.pyo # Python optimized bytecode files
.Python # macOS specific Python environment files
venv/
*.egg-info/ # Python package metadata directories
.pytest_cache/ # pytest cache directory

# Build and distribution files
dist/
build/

# Log and environment files
*.log
.env

# Local data and cache directories (specific to your project)
local_data/
ollama_data/
redis_data/
sentence_transformer_cache/

# Files not needed in the final image or specific to local development
docker-compose.yml # Exclude if only used for local development
*.DS_Store # macOS specific
Dockerfile # No need to copy the Dockerfile into the image itself

# Frontend/UI files (assuming this Docker image is for the backend only)
/uiux.html
/rough file
/package.json
/frontend/css/style1.css
/frontend/index1.html

# Data files (only exclude if they are NOT needed at runtime by the application)
# Based on your main.py, knowledge_base.txt might be loaded at runtime,
# so keep it if your app needs it. If it's only for building an index once,
# then you could exclude it. For now, we assume it IS needed.
# knowledge_base.txt
